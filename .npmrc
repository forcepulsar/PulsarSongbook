# vite-plugin-pwa@1.2.0 declares a peer range that stops at vite@7, but the
# project runs vite@8. Without this, `npm ci` fails with ERESOLVE -- including
# in Cloudflare Workers Builds, which installs before running the build.
legacy-peer-deps=true
